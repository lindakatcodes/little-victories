export interface Task {
  taskId: Number;
  taskComplete: Boolean;
  taskAction: String;
}

export interface RewardPicture {
  blur_hash: String;
  description: String;
  regUrl: String;
  smUrl: String;
  dlUrl: String;
  creditUrl: String;
}
